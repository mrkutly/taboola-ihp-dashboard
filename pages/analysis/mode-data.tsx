import ModeData from '../../components/analysis/ModeData';
import withPublisher from '../../lib/withPublisher';

const ModeDataPage: React.FunctionComponent = () => <ModeData />;

export default withPublisher(ModeDataPage);
