import ModeList from '../../components/analysis/ModeList';
import withPublisher from '../../lib/withPublisher';

const ModeListPage: React.FunctionComponent = () => <ModeList />;

export default withPublisher(ModeListPage);
